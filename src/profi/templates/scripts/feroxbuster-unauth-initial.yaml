---
metadata:
  filename: "feroxbuster-unauth-initial.yaml"
  tags: ["web", "api", "enum"]
  created: "2025-03-29"
  author: "Unknown"

content: |
  mkdir -p feroxbuster; feroxbuster -o feroxbuster/ferox_unauth_initial.log -w /usr/share/seclists/Discovery/Web-Content/combined_directories.txt --no-state -n --rate-limit <120|200> -u "http://<ip>:<port>"
