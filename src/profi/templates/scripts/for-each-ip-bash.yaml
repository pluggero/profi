---
metadata:
  filename: "for-each-ip-bash.yaml"
  tags: ["utils"]
  created: "2025-03-29"
  authors: ["Unknown"]

content: |
  touch ips.txt; \
  printf '%s\n' "Enumerating over the following ips:"; \
  cat ips.txt;

  cat ips.txt | while read ip
  do
      \$command
  done
