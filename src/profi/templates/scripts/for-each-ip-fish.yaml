---
metadata:
  filename: "for-each-ip-fish.yaml"
  tags: ["utils"]
  created: "2025-03-29"
  author: "Unknown"

content: |
  touch ips.txt; \
  echo "Enumerating over the following ips:"; \
  cat ips.txt;

  for ip in (cat ips.txt)
      \$command
  end
