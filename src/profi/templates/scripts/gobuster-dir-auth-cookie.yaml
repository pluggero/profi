metadata:
  filename: "gobuster-dir-auth-cookie.yaml"
  tags: ["web", "api"]
  created: "2025-03-29"
  author: "Unknown"

content: |
  mkdir -p gobuster; gobuster dir -x zip,pdf,php,html,txt,config -o gobuster/gobuster_auth.txt -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt -u http://<ip>:<port> -c "<cookie_name>=<cookie_value>"
