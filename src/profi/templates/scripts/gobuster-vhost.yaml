---
metadata:
  filename: "gobuster-vhost.yaml"
  tags: ["web", "api", "enum"]
  created: "2025-03-29"
  authors: ["Unknown"]

content: |
  mkdir -p gobuster; gobuster vhost -o gobuster/gobuster_vhost.txt -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-110000.txt --append-domain --domain <example.com> -u http://<ip>:<port>
