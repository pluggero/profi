metadata:
  filename: "gobuster-vhost.yaml"
  tags: ["web", "api"]
  created: "2025-03-29"
  author: "Unknown"

content: |
  mkdir -p gobuster; gobuster vhost -o gobuster/gobuster_vhost.txt -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-110000.txt --domain <example.com> -u http://<ip>:<port>
